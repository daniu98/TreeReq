# Copy to ../.env.local or frontend/.env (Vite loads from repo root via envDir).
# OAuth 2.0 *Web application* client ID from Google Cloud Console.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
